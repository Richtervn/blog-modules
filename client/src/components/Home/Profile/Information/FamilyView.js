import React from 'react';
import TabNavBar from 'components/TabNavBar';

export default () => (
  <div className="row profile-container profile-personal">
    <TabNavBar headers={['Father', 'Mother', 'Wife', 'Brother']} />

  </div>
);

