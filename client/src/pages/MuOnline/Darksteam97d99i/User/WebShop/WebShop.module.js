import {actionCreator} from 'helpers';
import services from '../../Darksteam97d99i.module';

const initialState = {};

export default (state = initialState, action) => {
  switch(action.type){
    default:
      return state;
  }
}