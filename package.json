{
  "name": "blog-modules",
  "version": "2.5.0",
  "description": "my-blog",
  "main": "index.js",
  "scripts": {
    "client:dev": "cd client && npm run start",
    "server:dev": "cd server && npm run start",
    "client:build": "cd client && npm run build",
    "server:build": "cd server && npm run build",
    "client:serve": "cd client && serve -s build",
    "server:serve": "cd server && npm run serve",
    "build": "start npm run client:build && start npm run server:build",
    "serve": "start npm run client:serve && start npm run server:serve",
    "dev": "start npm run client:dev && start npm run server:dev",
    "start": "npm run client:build && npm run server:build && npm run serve"
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true,
    "jsxBracketSameLine": true
  },
  "author": "VuPham",
  "license": "ISC"
}
