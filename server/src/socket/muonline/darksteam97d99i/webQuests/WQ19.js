/* non-realeased template */

export default class WQ19 {
	constructor(models, methods, membInfo, characters, banking, membCredits,webQuest, baseRecord){
		console.log('ok');
	}
	async check(){};

	async giveReward(){};
}
